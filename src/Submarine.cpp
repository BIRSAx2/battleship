#include "Submarine.h"

bool Submarine::MoveShip(Coordinates target) {
  return false;
}
std::vector<Coordinates> Submarine::ScanSurroundings(GameBoard opponent_board) {
  return std::vector<Coordinates>();
}
