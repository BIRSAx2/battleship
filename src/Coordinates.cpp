#include "Coordinates.h"

bool IsInBound(int min, int max){
    return true;
}

bool GetAdjacentCoordinates(Coordinates starting, Orientation orientation, int count);

static Coordinates GetCoordinates(std::string &coordinates);


